
<%= form_for @certificate, html: {class: "stickyfix page-header"} do |f| %>
    <%= f.label(:photo) %>
    <%= f.file_field :photo, class: "hidden" %>
    <%= f.submit "Upload Certificate Photo", class: "btn btn-default" %>
<% end %>